# Delete Tool

Delete a file or directory.

## Parameters

- `path` — relative path to the file or directory

## Rules

1. Use relative paths only
2. Confirm deletion with the user for important files
3. Directories are deleted recursively
4. Cannot delete the project root
