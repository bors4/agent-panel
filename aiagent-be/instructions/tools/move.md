# Move Tool

Rename or move a file/directory.

## Parameters

- `source` — relative path to the source file
- `destination` — relative path to the destination

## Rules

1. Use relative paths only
2. Both source and destination must be within the project
3. Use this for renaming files or moving them between directories
4. Parent directories are created automatically if needed
