# Create Directory Tool

Create a new directory.

## Parameters

- `path` — relative path for the new directory

## Rules

1. Use relative paths only
2. Parent directories are created automatically
3. Fails silently if directory already exists
