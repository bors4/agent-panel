# List Directory Tool

List contents of a directory.

## Parameters

- `path` — relative path to the directory (default: `.`)

## Rules

1. Use relative paths only
2. Default to current directory (`.`) if no path specified
3. Use this to explore project structure before making changes
