# Copy Tool

Copy a file or directory.

## Parameters

- `source` — relative path to the source
- `destination` — relative path to the destination

## Rules

1. Use relative paths only
2. Both source and destination must be within the project
3. Parent directories are created automatically if needed
4. Overwrites destination if it exists
